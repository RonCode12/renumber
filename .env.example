DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Basic auth gate for the whole app (defaults to renumber / medias if unset)
BASIC_AUTH_USER="renumber"
BASIC_AUTH_PASSWORD="medias"
